# Attendance Analysis Dashboard

A static, client-side dashboard for analyzing employee attendance CSV files.
Upload a CSV with `Date, Employee_ID, Name, Department, Status` columns to see
KPIs, attendance trends, status breakdown, and per-department attendance —
all parsed and computed directly in the browser (no backend required).
